cloud_storage_type="oci"

// Metric event config
metric.producer.id="pipeline.monitoring"
metric.producer.pid="dataproduct.metrics"
push.metrics.kafka=false
metric.kafka.broker="localhost:9092"
metric.kafka.topic="metric"

storage.key.config="oci_storage_key"
storage.secret.config="oci_storage_secret"
reports.storage.key.config="reports_oci_storage_key"
reports.storage.secret.config="reports_oci_storage_secret"